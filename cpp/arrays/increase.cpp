#include <iostream>
using namespace std;

int main()
{
    int length = 0;
    cin >> length;
    int massiv[length];
    // massiv[0] = 1;
    // printf("%i ", massiv[0]);
    for (int j = 0; j < length; j++)
    {

        printf("%i ", massiv[j]);
    }

    return 0;
}