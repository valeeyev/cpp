#include <iostream>
using namespace std;

int main()
{
    int length;
    cin >> length;
    int massiv[length];
    for (int i = 0; i < length; i++)
    {
        cin >> massiv[i];
    }

    return 0;
}
